# Copy to `.env` and fill in to switch from mock parser to real Claude.
# The demo runs without any of these.

# Anthropic API key — get one from https://console.anthropic.com
# When set, extract.py sends PDFs to Claude as multimodal documents
# instead of using the regex-based mock parser.
ANTHROPIC_API_KEY=
